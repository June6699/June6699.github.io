## 1 博客概览

这个仓库本质上是一个 **Hugo 静态博客**，主题基于 **PaperMod**，但并不是“直接套主题就完事”，而是在主题之上加了一层自己的 `layouts/`、`layouts/moments/`、`assets/css/extended/custom.css`、`data/projects.yaml`、`static/js/pretext-runtime.js`、`static/js/moments-gate.js` 和若干脚本，所以最终站点的行为更接近“PaperMod 作为底座，自定义模板作为业务层”。

### 1.1 技术栈

先看最核心的站点配置，博客的运行方式基本都从这里开始：

```toml
baseURL = "https://June6699.github.io/"
languageCode = "zh-cn"
defaultContentLanguage = "zh"
hasCJKLanguage = true
title = "June's Blog"
theme = "PaperMod"
googleAnalytics = "G-S7DXV0QV58"

[build]
  ignoreFiles = [
    "content/posts/images",
    "content/posts/images/",
    "content/moments/.*\\.(?i)(png|jpg|jpeg|webp|gif|bmp|avif)$",
  ]

[markup.goldmark.extensions.extras.mark]
  enable = true

[markup.goldmark.renderer]
  unsafe = true

[params]
  description = "Who Changes? Who Benefits!"
  author = "June"
  comments = true
  homeHeroImage = "/images/header_img/header.jpg"
  homeHeroTitle = "June's Blog"
  homeHeroSubtitle = "Who Changes? Who Benefits!"
  sidebarAboutTitle = "关于我"
  sidebarAboutText = "徐子昂 · 研究生 · 生信/前端/杂记"
  sidebarAvatar = "/images/底特律·变包.png"
  sidebarEmail = "3439394104@qq.com"
  siteBirthDate = "2026-01-01"
  mainSections = ["posts"]
  ShowToc = true
  TocOpen = false
  sidebarHideTagsOnHome = true
```

这里能看出五个关键点：

1. 博客生成器是 Hugo，主题是 `PaperMod`。
2. 文章主内容集中在 `content/posts/`，因为 `mainSections = ["posts"]`。
3. `==高亮==` 这种 Typora 风格语法已经通过 `extras.mark` 打开，渲染后会输出 `<mark>`。
4. 站点很多 UI 行为不是写死在模板里，而是由 `params` 驱动，比如首页头图、右侧侧栏、TOC、评论。
5. `content/posts/images` 与 `content/moments` 下的位图被 `ignoreFiles` 排除；图片以 **content 为唯一源**，由 `scripts/sync_images.py` 复制到 `static/images/`（该目录不提交），线上 CI 在 `hugo` 前会执行同步。

### 1.2 目录结构

这个仓库建议按下面这张图理解：

```text
June6699.github.io/
├─ content/                 # Hugo 内容源文件
│  ├─ posts/                # 博客文章
│  ├─ moments/              # 动态流条目（独立 section）
│  ├─ projects/_index.md    # projects 页入口
│  ├─ about.md              # 关于页
│  ├─ now.md                # 旧动态页，现已下线为草稿
│  ├─ uses.md               # 旧在用页，现已下线为草稿
│  └─ statistics.md         # 统计页，只负责声明 layout
├─ data/
│  └─ projects.yaml         # projects 页的数据源
├─ layouts/                 # 覆盖主题模板的“业务层”
│  ├─ _default/
│  ├─ moments/              # 动态流 list/single 模板
│  ├─ partials/
│  ├─ projects/
│  ├─ shortcodes/
│  └─ taxonomies/
├─ assets/
│  └─ css/extended/custom.css
├─ static/                  # 原样发布的静态资源
│  ├─ images/
│  ├─ js/moments-gate.js
│  ├─ js/pretext-runtime.js
│  └─ scripts/
├─ scripts/                 # 本地辅助脚本
│  ├─ sync_images.py
│  └─ sync_icons.py
├─ .github/workflows/       # CI：Hugo 构建与 GitHub Pages 部署
│  └─ hugo.yml              # 注入 GITALK、HUGO_MOMENTS_* 等 Secrets
├─ .env                     # 本地忽略文件，可存动态区口令哈希
├─ themes/PaperMod/         # 第三方主题底座
├─ hugo.toml                # 站点总配置
└─ run_server.bat           # 本地启动入口（可选加载 .env、可选参数生成 moments 哈希）
```

理解这个结构之后，后面所有逻辑都可以归纳成一句话：

> `content/` 提供内容，`data/` 提供结构化数据，`layouts/` 决定怎么渲染，`assets/` 决定样式，`static/` 决定静态文件直出，`scripts/` 负责本地同步和启动。

## 2 页面装配逻辑

### 2.1 最外层骨架：`baseof.html`

Hugo 会先进入主题的基础模板，再把自定义页面内容塞进 `main` 块里。这个骨架很重要，因为它决定了所有页面共有的 `<head>`、页头、页脚和主体容器。

```html
<!DOCTYPE html>
<html lang="{{ site.Language }}" dir="{{ .Language.LanguageDirection | default "auto" }}" data-theme="auto">
<head>
    {{- partial "head.html" . }}
</head>
<body id="top">
    {{ partialCached "header.html" . .Page -}}
    <main class="main">
        {{- block "main" . }}{{ end }}
    </main>
    {{ partialCached "footer.html" . .Layout .Kind (.Param "hideFooter") (.Param "ShowCodeCopyButtons") -}}
</body>
</html>
```

这里的含义可以拆成三层：

#### 2.1.1 Hugo 的模板继承

`block "main"` 表示“主题定义好壳子，具体页面自己填内容”。所以你在 `layouts/_default/list.html`、`layouts/_default/single.html`、`layouts/projects/list.html` 里看到的 `define "main"`，本质上都是往这个壳子里填东西。

#### 2.1.2 自定义模板覆盖主题模板

虽然 `baseof.html` 来自主题，但 `header.html`、`footer.html`、`comments.html`、`toc.html` 等 partial，仓库自己也提供了同名文件：

```text
layouts/partials/header.html
layouts/partials/footer.html
layouts/partials/comments.html
layouts/partials/toc.html
```

Hugo 的规则是：**项目根目录下的 `layouts/` 优先级高于主题里的 `layouts/`**。所以这里实际上是在“借用主题框架，但替换部分器官”。

### 2.2 头部与菜单：`layouts/partials/header.html`

这个文件控制站点左上角标题、主题切换按钮、语言切换和顶部导航。

```html
<a href="{{ "/" | relLangURL }}" accesskey="h" title="{{ $label_text }} (Alt + H)">
  {{- $label_text -}}
</a>

{{- range site.Menus.main }}
{{- $raw := .URL -}}
{{- $href := $raw -}}
{{- if and (not (strings.HasPrefix $raw "http://")) (not (strings.HasPrefix $raw "https://")) -}}
  {{- $href = ($raw | relLangURL) -}}
{{- end -}}
<li>
  <a href="{{ $href }}" title="{{ .Title | default .Name }}">
    <span {{- if $currentPage.IsMenuCurrent "main" . }} class="active" {{- end }}>
      {{- .Name -}}
    </span>
  </a>
</li>
{{- end }}
```

这里最值得注意的是 `relLangURL`。它不是简单拼路径，而是让链接尽量保持“站内相对语义”，这样本地 `http://localhost:1313/` 跑起来时，不容易被线上 `baseURL` 直接带偏。

### 2.3 页脚与前端增强：`layouts/partials/footer.html`

页脚不只是展示版权，它还挂了很多前端交互脚本。

```html
<script>
    let menu = document.getElementById('menu');
    if (menu) {
        const scrollPosition = localStorage.getItem("menu-scroll-position");
        if (scrollPosition) {
            menu.scrollLeft = parseInt(scrollPosition, 10);
        }
        menu.onscroll = function () {
            localStorage.setItem("menu-scroll-position", menu.scrollLeft);
        };
    }

    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener("click", function (e) {
            e.preventDefault();
            var id = this.getAttribute("href").substr(1);
            document.querySelector(`[id='${decodeURIComponent(id)}']`).scrollIntoView({
                behavior: "smooth"
            });
        });
    });
</script>
```

这一段做了三件事：

#### 2.3.1 顶部菜单滚动位置记忆

横向菜单如果太长，刷新后还能回到上次滚动位置，状态存进 `localStorage`。

#### 2.3.2 锚点平滑滚动

点击目录、页内链接时，不会瞬移，而是平滑滚动到目标标题。

#### 2.3.3 后续脚本挂载点

同一个文件里还包括：

```html
<script>
    var mybutton = document.getElementById("top-link");
    window.onscroll = function () {
        if (document.body.scrollTop > 800 || document.documentElement.scrollTop > 800) {
            mybutton.style.visibility = "visible";
            mybutton.style.opacity = "1";
        } else {
            mybutton.style.visibility = "hidden";
            mybutton.style.opacity = "0";
        }
    };
</script>

<script>
    document.getElementById("theme-toggle").addEventListener("click", () => {
        const html = document.querySelector("html");
        if (html.dataset.theme === "dark") {
            html.dataset.theme = 'light';
            localStorage.setItem("pref-theme", 'light');
        } else {
            html.dataset.theme = 'dark';
            localStorage.setItem("pref-theme", 'dark');
        }
    })
</script>
```

分别对应“返回顶部按钮”和“亮暗主题切换”。

## 3 列表页与文章页

### 3.1 首页和列表页：`layouts/_default/list.html`

首页、文章列表页、标签下的文章列表页，核心都走这个模板。

```html
<div class="page-with-sidebar">
<div class="list-with-sidebar">
  <div class="list-main">
    {{- if .IsHome }}
      {{- $img := site.Params.homeHeroImage | default "" -}}
      {{- if $img }}
      <section class="home-hero">
        <div class="home-hero-inner">
          <img class="home-hero-img" src="{{ $img | relURL }}" alt="home hero" loading="lazy" />
          <div class="home-hero-text">
            <h1 class="home-hero-title">{{ site.Params.homeHeroTitle | default site.Title }}</h1>
            <p class="home-hero-subtitle">{{ site.Params.homeHeroSubtitle }}</p>
          </div>
        </div>
      </section>
      {{- end }}
    {{- end }}
```

这说明首页头图完全由 `hugo.toml` 的参数驱动，而不是写死在首页模板里。

下面是真正的文章列表逻辑：

```html
{{- $pages := union .RegularPages .Sections }}
{{- if .IsHome }}
  {{- $pages = where site.RegularPages "Type" "in" site.Params.mainSections }}
  {{- $pages = where $pages "Params.hiddenInHomeList" "!=" "true" }}
{{- end }}
{{- $paginator := .Paginate $pages }}

{{- range $index, $page := $paginator.Pages }}
  <article class="post-entry">
    <header class="entry-header">
      <h2 class="entry-hint-parent">{{- .Title }}</h2>
    </header>
    <div class="entry-content">
      <p>{{ .Summary | plainify | htmlUnescape }}{{ if .Truncated }}...{{ end }}</p>
    </div>
    <footer class="entry-footer">
      {{- partial "post_meta.html" . -}}
    </footer>
    <a class="entry-link" aria-label="post link to {{ .Title | plainify }}" href="{{ .RelPermalink }}"></a>
  </article>
{{- end }}
```

它的运行逻辑是：

#### 3.1.1 首页不是“渲染所有页面”

首页会先筛 `site.Params.mainSections`，这里配置的是 `posts`，所以首页只显示文章，不显示 `about`、`moments`、`projects` 这些单页或 section。

#### 3.1.2 Hugo 自动摘要被拿来当列表简介

`.Summary` 是 Hugo 生成或截断的摘要，因此文章卡片不需要单独写一个 `excerpt` 字段也能工作。

#### 3.1.3 列表页右边是自定义侧栏

模板尾部直接插入：

```html
{{- partial "site_sidebar.html" . -}}
```

也就是说，首页和一般列表页的双栏布局不是主题默认能力，而是这个仓库自己扩出来的。

### 3.2 文章页：`layouts/_default/single.html`

文章页最关键的逻辑是“什么时候显示右侧目录，什么时候显示普通侧栏，什么时候变成窄栏单页”。

```html
{{- $showToc := (.Param "ShowToc") -}}
{{- $tocHTML := .TableOfContents -}}
{{- $hasToc := (and $showToc (findRE "href=\\\"#" $tocHTML)) -}}
{{- $hideSidebar := (.Param "hideSidebar") | default false -}}
{{- $isPost := in site.Params.mainSections .Section -}}
{{- $useWideSidebar := or $hasToc (and $isPost (not $hideSidebar)) -}}
```

这段是整套文章页布局判断的核心：

#### 3.2.1 有目录就优先走 TOC 侧栏

只要当前页面有目录且 `ShowToc = true`，右边就放目录。

#### 3.2.2 没目录但属于 `posts` 也保留宽布局

普通文章如果没目录，也仍然可以显示博客侧栏，不会立刻退化成纯单栏。

#### 3.2.3 `about` 这类普通单页走窄栏

如果不满足 `$useWideSidebar`，模板会切到：

```html
<div class="single-static-wrap">
  <article class="post-single single-static">
```

这就是为什么“文章页”和“关于页”虽然都是单页，但视觉宽度和右侧结构并不一样。`moments` 没走这条默认分支，而是单独用了 `layouts/moments/` 下的 section 模板。

文章正文的渲染也做了一个定制：

```html
{{- if .Content }}
<div class="post-content">
  {{- $content := .Content -}}
  {{- if not (.Param "disableAnchoredHeadings") }}
  {{- partial "anchored_headings.html" $content -}}
  {{- else }}{{ $content }}{{ end }}
</div>
{{- end }}
```

它会在默认情况下给标题自动加锚点，除非页面 front matter 里显式关闭 `disableAnchoredHeadings`。

## 4 侧栏、目录、评论这些“函数”

### 4.1 侧栏函数：`layouts/partials/site_sidebar.html`

这个 partial 可以把它看作“页面右侧的 UI 函数”。不同页面传进来的是同一个 `.`，但输出会因为页面类型不同而变化。

```html
<section class="sidebar-card sidebar-about">
  <div class="sidebar-about-row">
    {{- with site.Params.sidebarAvatar }}
    <img class="sidebar-avatar" src="{{ . | relURL }}" alt="avatar" loading="lazy" />
    {{- end }}
    <div class="sidebar-about-meta">
      <h3 class="sidebar-title">{{ site.Params.sidebarAboutTitle | default "关于" }}</h3>
      <p class="sidebar-text">{{ site.Params.sidebarAboutText | default (site.Params.description | default site.Title) }}</p>
    </div>
  </div>
```

这个部分完全由 `site.Params` 提供数据，所以改 `hugo.toml` 就能改侧栏卡片内容。

首页还会额外显示快捷入口：

```html
{{- if and .IsHome site.Params.homeQuickLinks }}
<nav class="home-quick-links" aria-label="常用入口">
  {{- range first 3 site.Params.homeQuickLinks }}
    <a href="{{ $href | relURL }}">{{ .name }}</a>
  {{- end }}
</nav>
{{- end }}
```

热门标签模块也不是主题原样输出，而是先手动筛选：

```html
{{- $items := slice -}}
{{- range $termName, $taxonomy := site.Taxonomies.tags -}}
  {{- $count := len $taxonomy.Pages -}}
  {{- if ge $count 2 -}}
    {{- $items = $items | append (dict "title" $taxonomy.Page.Title "permalink" $taxonomy.Page.RelPermalink "count" $count) -}}
  {{- end -}}
{{- end -}}
{{- range first 12 (sort $items "count" "desc") -}}
  <a class="sidebar-tag" href="{{ .permalink }}">{{ .title }} <sup>{{ .count }}</sup></a>
{{- end -}}
```

也就是：

#### 4.1.1 只显示出现至少 2 次的标签

避免右栏被冷门标签刷屏。

#### 4.1.2 只取前 12 个

控制右栏高度。

#### 4.1.3 首页可隐藏标签卡

这由 `sidebarHideTagsOnHome = true` 控制。

### 4.2 目录函数：`layouts/partials/toc.html`

这个文件很像“服务端生成目录树的函数”。它不是完全依赖 Hugo 内置 TOC，而是自己解析标题标签。

```html
{{- $headers := findRE "<h[1-6].*?>(.|\n])+?</h[1-6]>" .Content -}}
{{- $has_headers := ge (len $headers) 1 -}}
{{- if $has_headers -}}
<div class="toc toc-from-h1">
    <div class="inner">
            {{- if (.Param "UseHugoToc") }}
            {{- .TableOfContents -}}
            {{- else }}
```

接下来它会循环标题层级，手动拼 `<ul><li>` 的嵌套结构：

```html
{{- $id := index (findRE "(id=\"(.*?)\")" $header 9) 0 }}
{{- $cleanedID := replace (replace $id "id=\"" "") "\"" "" }}
{{- $header := replaceRE "<h[1-6].*?>((.|\n])+?)</h[1-6]>" "$1" $header -}}
{{- $headerText := $header | plainify -}}

<li>
  <a href="#{{- $cleanedID -}}" aria-label="{{- $headerText -}}" title="{{- $headerText -}}">
    {{- $header | plainify | safeHTML -}}
  </a>
</li>
```

这个实现的特点是：

#### 4.2.1 目录从 H1 就开始列

不是很多主题默认的“只从 H2 开始”。

#### 4.2.2 目录结构可控

因为是手拼 HTML，所以样式和层级规则都更容易按自己的需求改。

#### 4.2.3 代价是模板更复杂

你看到的 `Scratch`、正则、层级补 `<ul>` / 收 `<ul>`，本质上都是在 Hugo 模板语言里模拟一个树构造器。

### 4.3 目录高亮与阅读进度：`layouts/partials/extend_footer.html`

目录 HTML 只是第一步，真正让 TOC “跟着阅读位置高亮”的，是页脚追加的 JS。

```html
const tocLinks = sidebar.querySelectorAll('.toc .inner a[href^="#"]');
const headings = postContent.querySelectorAll('h1[id], h2[id], h3[id], h4[id], h5[id], h6[id]');

function setActive(id) {
    tocLinks.forEach(function(a) {
        const href = a.getAttribute('href');
        if (href === '#' + id) {
            a.classList.add('active');
        } else {
            a.classList.remove('active');
        }
    });
}
```

它一边用 `IntersectionObserver` 观察标题，一边用 `scroll` 事件做补偿：

```html
const observer = new IntersectionObserver(
    function(entries) {
        let best = null;
        entries.forEach(function(entry) {
            if (!entry.isIntersecting) return;
            const id = entry.target.id;
            if (!best || entry.boundingClientRect.top <= 80)
                best = { id: id, top: entry.boundingClientRect.top };
        });
        if (best) setActive(best.id);
    },
    { root: null, rootMargin: '-80px 0px -60% 0px', threshold: [0, 0.25, 0.5, 1] }
);
```

同时它还加了阅读进度条：

```html
function update(){
  var doc = document.documentElement;
  var scrollTop = (window.pageYOffset || doc.scrollTop || 0);
  var scrollHeight = Math.max(doc.scrollHeight, document.body.scrollHeight || 0);
  var clientHeight = doc.clientHeight || window.innerHeight || 0;
  var total = Math.max(1, scrollHeight - clientHeight);
  var p = Math.max(0, Math.min(1, scrollTop / total));
  var bar = document.getElementById('reading-progress');
  if (bar) bar.style.width = (p * 100).toFixed(2) + '%';
}
```

所以文章页的“目录联动”和“顶部细进度条”是两套独立功能，共同挂在 `extend_footer.html`。

### 4.4 评论函数：`layouts/partials/comments.html`

评论区走的是 Gitalk，而且优先从环境变量取密钥。

```html
{{- $envClientID := getenv "GITALK_CLIENT_ID" | strings.TrimSpace -}}
{{- $envClientSecret := getenv "GITALK_CLIENT_SECRET" | strings.TrimSpace -}}
{{- $clientID := $envClientID | default ($gitalk.clientID | default "") | strings.TrimSpace -}}
{{- $clientSecret := $envClientSecret | default ($gitalk.clientSecret | default "") | strings.TrimSpace -}}
{{- if and $gitalk $gitalk.enable $clientID $clientSecret -}}
```

再通过延迟加载的方式把 CSS、MD5、Gitalk 主脚本依次拉进来：

```html
function loadCss(href){
  var link = document.createElement('link');
  link.rel = 'stylesheet';
  link.href = href;
  document.head.appendChild(link);
}

function loadJs(src, cb){
  var s = document.createElement('script');
  s.async = true;
  s.src = src;
  s.onload = cb;
  document.body.appendChild(s);
}
```

真正实例化评论时，会把当前路径做 MD5 当作评论线程 ID：

```html
var path = (location.pathname || '/');
var id = (typeof md5 === 'function') ? md5(path) : path;

var gitalk = new Gitalk({
  clientID: dequote({{ $clientID | jsonify }}),
  clientSecret: dequote({{ $clientSecret | jsonify }}),
  repo: dequote({{ (cond (ne $envRepo "") $envRepo $gitalk.repo) | jsonify }}),
  owner: dequote({{ (cond (ne $envOwner "") $envOwner $gitalk.owner) | jsonify }}),
  admin: admin,
  redirect_uri: redirectURI,
  distractionFreeMode: {{ $gitalk.distractionFreeMode | default true }},
  id: id
});
```

也就是说，这里的“函数逻辑”是：

#### 4.4.1 配置读取优先级

环境变量 > `hugo.toml`。

#### 4.4.2 评论线程唯一键

页面路径哈希值。

#### 4.4.3 加载策略

空闲时再加载，尽量不阻塞首屏。

## 5 样式系统

### 5.1 样式是怎么进站点的

这个博客虽然只有一个 `assets/css/extended/custom.css`，但它不是手工 `<link>` 进去的，而是被 PaperMod 的资源流水线自动拼进去。

```html
{{- $common := (resources.Match "css/common/*.css") | resources.Concat "assets/css/common.css" }}
{{- $extended := (resources.Match "css/extended/*.css") | resources.Concat "assets/css/extended.css" | resources.Minify }}
{{- $stylesheet := (slice $license_css $core $extended) | resources.Concat "assets/css/stylesheet.css"  }}
```

这意味着：

#### 5.1.1 `assets/css/extended/*.css` 都会自动参与构建

所以仓库只要放一个 `custom.css` 进去，就能覆盖主题默认样式。

#### 5.1.2 样式不是静态复制，而是 Hugo Pipes 处理

会经历拼接、压缩、可选指纹化。

### 5.2 全局设计变量：`assets/css/extended/custom.css`

这个文件先定义了一套自己的视觉变量，亮色和暗色分别有两套主题值。

```css
:root {
    --accent: #2563eb;
    --accent-muted: #3b82f6;
    --primary: #1e293b;
    --secondary: #475569;
    --content: #1a1a2e;
    --border: #e2e8f0;
    --entry: #f8fafc;
    --nav-width: 1240px;
    --content-column-max: 820px;
    --list-sidebar-width: 280px;
    --toc-sidebar-width: 218px;
    --projects-surface: #f4f1eb;
    --projects-accent: #c2410c;
}

:root[data-theme="dark"] {
    --accent: #60a5fa;
    --primary: #e3f7ff;
    --secondary: #9ec5fe;
    --entry: #242526;
    --theme: #18191a;
    --projects-surface: #1c1b19;
    --projects-accent: #fb923c;
}
```

这套变量的作用不是只给颜色换皮，而是顺带把布局尺寸一起参数化了，比如正文宽度、TOC 宽度、列表侧栏宽度都在这里定义。

### 5.3 首页与列表页样式

首页和普通列表页的关键是“双栏 + 顶部 Hero 图”。

```css
.list-with-sidebar {
    display: grid;
    grid-template-columns: minmax(0, 1fr) var(--list-sidebar-width, 280px);
    gap: var(--list-sidebar-gap, 1.25rem);
    align-items: start;
}

.home-hero-inner {
    position: relative;
    overflow: hidden;
    border-radius: 14px;
    border: 1px solid var(--border);
    background: var(--entry);
}

.home-hero-img {
    width: 100%;
    height: 240px;
    object-fit: cover;
}
```

对应到页面效果就是：

#### 5.3.1 左侧内容流，右侧信息流

文章卡片在左边，关于我/标签/快捷入口在右边。

#### 5.3.2 首页头图不是背景图 hack

而是真 `<img>` + 绝对定位文字层，适配更直接。

### 5.4 文章页样式

文章页是“正文卡片 + 右侧 TOC”这套布局：

```css
.single-with-sidebar {
    display: grid;
    grid-template-columns: minmax(0, 1fr) var(--toc-sidebar-width, 218px);
    column-gap: var(--single-sidebar-gap, 1.35rem);
}

.single-with-sidebar .post-single {
    background: var(--entry);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-md);
    padding: 1.2rem 1.35rem;
}

.toc-sidebar {
    position: sticky;
    top: calc(var(--header-height, 60px) + 1rem);
    max-height: calc(100vh - var(--header-height, 60px) - 2rem);
    overflow-y: auto;
}
```

这套设计的重点是：

#### 5.4.1 TOC 固定，正文滚动

右栏 `sticky`，所以长文章浏览体验更稳定。

#### 5.4.2 正文是“卡片化”而不是裸文本

边框、圆角、阴影都强化了独立阅读区的感觉。

### 5.5 正文内容样式

正文里大量细节都被单独美化了。

#### 5.5.1 表格

```css
.post-content table {
    width: fit-content;
    max-width: 100%;
    margin: 1.5rem auto;
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow-md);
    display: block;
    overflow-x: auto;
}
```

也就是表格默认居中、带圆角阴影，并且小屏幕下可以横向滚动，而不是把版面挤炸。

#### 5.5.2 行内代码、引用、代码块语言标签

```css
.post-content :not(pre) > code {
    background: var(--code-bg, rgba(0,0,0,0.04));
    color: var(--as-inline-code);
    padding: 0.22em 0.45em;
    border-radius: 6px;
}

.post-content blockquote {
    border-left: 4px solid var(--as-blockquote-border);
    color: var(--as-blockquote-text);
    padding: 0.75rem 1.25rem;
}

.post-content .highlight:has(code.language-python)::before {
    content: "Python";
    background: #3581b8;
    color: #fff;
}
```

这些样式说明作者不是只改了整体布局，而是把文章阅读体验当成重点维护对象。

### 5.6 Callout 与短代码

这一块现在不只依赖 shortcode，还把 Typora / GitHub 风格的警示块语法直接接进了 Hugo 的 render hook。也就是说，正文里既可以写：

```md
==重点==

> [!IMPORTANT]
>
> 这是重要说明
```

也可以继续写现有 shortcode：

```md
{{< important >}}
这是重要说明
{{< /important >}}
```

两种写法最后都会汇到同一套 `callout` 组件，不再在页面上显示原始的 `[!IMPORTANT]` 标记，而是只显示图标 + 标题 + 正文。

先看 `render hook`，它专门负责把 `> [!TYPE]` 这种 blockquote alert 转成统一组件：

```go-html-template
{{- $labels := dict
  "note" "注意"
  "tip" "建议"
  "important" "重要"
  "warning" "警示"
  "caution" "注意风险"
-}}
{{- if eq .Type "alert" -}}
  {{- $alertType := lower .AlertType -}}
  {{- $alertLabel := or (index $labels $alertType) (title $alertType) -}}
  {{- partial "callout.html" (dict
    "type" $alertType
    "title" (or .AlertTitle $alertLabel)
    "body" .Text
  ) -}}
{{- else -}}
  <blockquote>
    {{ .Text }}
  </blockquote>
{{- end -}}
```

真正复用的公共骨架在 partial 里，render hook 和各个 shortcode 都走这里：

```go-html-template
{{- $kind := .type | default "note" -}}
{{- $defaults := dict
  "note" "注意"
  "tip" "建议"
  "important" "重要"
  "warning" "警示"
  "caution" "注意风险"
  "reminder" "提醒"
-}}
{{- $icons := dict
  "important" `<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M8 10h8"/><path d="M8 14h5"/><path d="M6 19h8a4 4 0 0 0 4-4V9a4 4 0 0 0-4-4H10a4 4 0 0 0-4 4z"/><path d="m6 19-2 2v-5"/></svg>`
  "warning" `<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 9v4"/><path d="M12 17h.01"/><path d="M10.3 3.8 2.9 17a2 2 0 0 0 1.7 3h14.8a2 2 0 0 0 1.7-3L13.7 3.8a2 2 0 0 0-3.4 0z"/></svg>`
-}}
{{- $title := .title | default "" -}}
{{- if not $title -}}
  {{- $title = index $defaults $kind | default (title $kind) -}}
{{- end -}}
{{- $icon := index $icons $kind | default (index $icons "important") -}}
<div class="callout callout-{{ $kind }}" data-callout="{{ $kind }}" role="note" aria-label="{{ $title }}">
  <div class="callout-head">
    <span class="callout-icon">{{ $icon | safeHTML }}</span>
    <span class="callout-title">{{ $title }}</span>
  </div>
  <div class="callout-body">{{ .body }}</div>
</div>
```

shortcode 这一层就只保留“传参 + 复用 partial”的职责，例如 `important`：

```go-html-template
{{- $title := "" -}}
{{- if .IsNamedParams -}}
  {{- $title = .Get "title" -}}
{{- else if ge (len .Params) 1 -}}
  {{- $title = .Get 0 -}}
{{- end -}}
{{- partial "callout.html" (dict
  "type" "important"
  "title" $title
  "body" (.Inner | markdownify)
) -}}
```

样式层则被改成“公共变量 + 类型覆写”的结构。共同点都在基类里，比如间距、左侧竖条、标题行、图标尺寸；差异只通过 `--callout-accent`、`--callout-bg`、`--callout-title-color` 这些变量覆盖：

```css
.post-content .callout {
    --callout-accent: #64748b;
    --callout-bg: rgba(100, 116, 139, 0.1);
    --callout-title-color: #334155;
    --callout-body-color: var(--content);
    margin: 1.25rem 0;
    padding: 1rem 1.2rem 1rem 1.25rem;
    border-radius: 0 16px 16px 0;
    border-left: 4px solid var(--callout-accent);
    background:
        linear-gradient(
            135deg,
            color-mix(in srgb, var(--callout-bg) 82%, var(--entry) 18%),
            color-mix(in srgb, var(--callout-bg) 94%, var(--entry) 6%)
        );
    color: var(--callout-body-color);
    box-shadow: 0 10px 24px rgba(15, 23, 42, 0.06);
}

.post-content .callout .callout-head {
    margin: 0 0 0.65rem 0;
    display: flex;
    gap: 0.72rem;
    align-items: center;
}

.post-content .callout .callout-icon {
    display: inline-flex;
    width: 1.4rem;
    height: 1.4rem;
    flex: 0 0 auto;
    color: var(--callout-accent);
}

.post-content .callout .callout-title {
    color: var(--callout-title-color);
    font-size: 1rem;
    font-weight: 800;
    line-height: 1.3;
}
```

各类型再做微调，例如：

```css
.post-content .callout-important {
    --callout-accent: #7c3aed;
    --callout-bg: rgba(124, 58, 237, 0.12);
    --callout-title-color: #6d28d9;
    --callout-body-color: #4c1d95;
}

.post-content .callout-warning {
    --callout-accent: #d97706;
    --callout-bg: rgba(217, 119, 6, 0.12);
    --callout-title-color: #b45309;
    --callout-body-color: #78350f;
}

.post-content .callout-caution {
    --callout-accent: #dc2626;
    --callout-bg: rgba(220, 38, 38, 0.1);
    --callout-title-color: #b91c1c;
    --callout-body-color: #7f1d1d;
}
```

这套实现的分层关系可以概括成：

1. `hugo.toml` 负责打开 `==高亮==` 的 `mark` 扩展。
2. `render-blockquote.html` 负责识别 `> [!IMPORTANT]` 这种 alert blockquote。
3. `layouts/partials/callout.html` 负责统一输出 HTML 结构。
4. 各个 shortcode 只负责把 `.Inner` 和可选标题转交给公共 partial。
5. `custom.css` 负责共享骨架与各类型视觉差异。

### 5.7 诗词与特殊内容样式

博客里有诗词、祝福、年终总结等偏中文排版的内容，所以 CSS 也专门写了这部分。

```css
.post-content .poem-content .poem-lines {
    font-size: 1.25em;
    line-height: 2.2;
    letter-spacing: 0.12em;
    color: var(--content);
    font-family: "SimSun", "宋体", "STSong", serif;
}

.post-content .poem-content ruby rt {
    font-size: 0.45em;
    line-height: 1;
    color: var(--secondary);
}
```

这说明这个博客不是只面向技术文章，它也在兼容古诗词、注音、节日文案等内容形态。

## 6 Projects 页面

### 6.1 数据源：`data/projects.yaml`

`projects` 不是一篇普通 Markdown 列表，而是一个“数据驱动页面”。数据文件长这样：

```yaml
items:
  - title: "ASCII Generator（Python）"
    summary: "图片/视频转 ASCII，依赖 OpenCV 与 Pillow。含环境配置、命令示例与脚本下载路径。"
    stack: ["Python", "OpenCV", "Pillow"]
    status: "Local Tool"
    demoUrl: ""
    docsUrl: "/scripts/ascii-generator/guide.html"
  - title: "ASCII 字符视频引擎（浏览器）"
    summary: "本地选视频即可解析、播放、存档与导出 MP4。克隆到本地时请用 static/scripts/vid2ascii-gif 下脚本或说明启动，勿直接双击 HTML。"
    stack: ["JavaScript", "FFmpeg", "WASM"]
    status: "Browser Demo"
    demoUrl: "/scripts/vid2ascii-gif/index.html"
    docsUrl: "/scripts/vid2ascii-gif/guide.html"
```

这个设计是对的，因为 `projects` 本质是“项目卡片列表”，比起写死在 Markdown 里，更适合放在 `data/` 下统一维护。

### 6.2 页面入口：`content/projects/_index.md`

这个文件负责给页面提供标题、描述和一些 front matter 选项：

```md
---
title: "我的项目"
description: "本地 Python 工具与浏览器内演示，互链说明见各子页。"
ShowToc: false
comments: false
disableAnchoredHeadings: true
---

本站托管两个小项目：**Python 本地转换**（含部署说明与可下载脚本）与**纯前端**（FFmpeg WASM，无需后端）。
```

它只负责“页面元信息 + 导语”，真正的项目卡片不在这里写。

### 6.3 页面模板：`layouts/projects/list.html`

真正渲染卡片的是这个模板：

```html
{{- with site.Data.projects }}
{{- with .items }}
<div class="projects-hub">
  {{- range . }}
  <article class="project-card">
    <span class="project-card__status">{{ .status }}</span>
    <h2 class="project-card__title">{{ .title }}</h2>
    <p class="project-card__summary">{{ .summary }}</p>
    {{- with .stack }}
    <ul class="project-card__stack">
      {{- range . }}
      <li>{{ . }}</li>
      {{- end }}
    </ul>
    {{- end }}
    <div class="project-card__actions">
      {{- with .demoUrl }}
      <a class="project-card__btn project-card__btn--demo" href="{{ . | relURL }}" target="_blank" rel="noopener noreferrer">Demo</a>
      {{- end }}
      {{- with .docsUrl }}
      <a class="project-card__btn project-card__btn--docs" href="{{ . | relURL }}" target="_blank" rel="noopener noreferrer">Docs</a>
      {{- end }}
    </div>
  </article>
  {{- end }}
</div>
{{- end }}
{{- end }}
```

运行逻辑非常清楚：

#### 6.3.1 页面文案来自 `.Content`

也就是 `content/projects/_index.md` 的正文。

#### 6.3.2 卡片列表来自 `site.Data.projects.items`

也就是 `data/projects.yaml`。

#### 6.3.3 按钮是否显示由字段是否为空决定

`demoUrl` 为空就不渲染 Demo 按钮，`docsUrl` 有值就渲染 Docs。

### 6.4 Projects 专属样式

`custom.css` 给 `projects` 单独做了一套偏“实验室卡片”的视觉风格。

```css
.projects-page-wrap {
    width: 100%;
    max-width: min(960px, 100%);
    margin: 0 auto;
    padding: 1rem 0.75rem 2.5rem;
    background: var(--projects-surface);
    border: 1px solid var(--projects-surface-border);
    border-radius: var(--radius-lg);
}

.projects-hub {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1.25rem;
}

.project-card__btn {
    display: inline-flex;
    min-width: 5rem;
    padding: 0.45rem 0.9rem;
    border: 1px solid var(--projects-accent);
    color: var(--projects-accent);
}
```

和普通文章列表相比，它不是一个个 `post-entry`，而是更接近产品页/作品集页。

## 7 统计页与标签页

### 7.1 统计页：`content/statistics.md` + `layouts/_default/statistics.html`

`content/statistics.md` 本身非常轻，只负责声明用哪个 layout：

```md
---
title: "统计"
layout: "statistics"
---

统计信息由下方布局自动生成。
```

真正的统计逻辑都在模板里：

```html
{{- $posts := where site.RegularPages "Section" "posts" }}
{{- $total := len $posts }}
{{- $.Scratch.Set "totalChars" 0 }}
{{- range $posts }}{{ $.Scratch.Add "totalChars" (countrunes (plainify .Content)) }}{{ end }}
{{- $totalChars := $.Scratch.Get "totalChars" }}
{{- $currentYear := now.Year }}
{{- $postsThisYear := where $posts "Date.Year" $currentYear }}
```

这里的计算项包括：

#### 7.1.1 文章总数

`len $posts`

#### 7.1.2 总字数

遍历所有文章，用 `countrunes` 统计字符数。

#### 7.1.3 本年文章数

按 `Date.Year` 过滤。

#### 7.1.4 站点运行天数

```html
{{- with site.Params.siteBirthDate }}
{{- $birth := time . }}
{{- $days := div (int (now.Sub $birth).Seconds) 86400 }}
<div class="stat-card stat-card--highlight">
  <span class="stat-number">{{ $days }}</span>
  <span class="stat-label">已运行天数</span>
</div>
{{- end }}
```

这就是为什么 `hugo.toml` 里专门定义了 `siteBirthDate`。

### 7.2 标签页：`layouts/_default/terms.html` / `layouts/taxonomies/terms.html`

标签页不是主题默认那个简单版本，而是手工做了两套排序视图：按热度、按名称。

```html
{{- $termsAlpha := .Data.Terms.Alphabetical }}
{{- $termsCount := .Data.Terms.ByCount }}

<section class="terms-by-count">
  <h2 class="terms-subtitle">按热度</h2>
  <ul class="terms-tags terms-tags--by-count">
    {{- range $termsCount }}
```

计数不是直接信任 `.Count`，而是又手动精确遍历了一次：

```html
{{- $termKey := lower $termName }}
{{- $exactCount := 0 }}
{{- range site.RegularPages }}
  {{- with .Params.tags }}
    {{- range . }}
      {{- if eq (lower .) $termKey }}{{ $exactCount = add $exactCount 1 }}{{ end }}
    {{- end }}
  {{- end }}
{{- end }}
```

这么做的意义是统一大小写口径，避免标签统计出现大小写重复或偏差。

## 8 静态资源与辅助脚本

### 8.1 图片路径改写：`layouts/_default/_markup/render-image.html`

博客正文里的图片路径不是直接照 Markdown 原文输出，而是先过这个 render hook：

文章内 `./images/...` 会改为 `/images/...`；**section 为 `moments`** 时，`./foo.png` 会改为 `/images/moments/foo.png`（源文件在 `content/moments/`）。

也就是说，作者写文章时可以写：

```md
![示意图](./images/某文章/xx.png)
```

生成网页时会自动变成：

```html
<img src="/images/某文章/xx.png" alt="示意图" loading="lazy" />
```

这就是“写作体验”和“发布路径”两边兼顾的关键点。

### 8.2 同步图片：`scripts/sync_images.py`

**唯一源目录（避免与 `static/images` 双份占仓库体积）：**

1. **文章配图**：`content/posts/images/<子目录>/` → `static/images/<子目录>/`（Markdown 里仍写 `./images/...`，由 render hook 变成 `/images/...`）。
2. **动态配图**：`content/moments/` 下任意层级的 `.png/.jpg/...` → `static/images/moments/<相对路径>`。`front matter` 的 `photos` 可写 `flower.jpg` 或 `./flower.jpg`（模板映射为 `/images/moments/...`）；动态正文里 `![x](./a.png)` 由 `render-image` 在同 section 下映射到 `/images/moments/a.png`。

`hugo.toml` 的 `ignoreFiles` 会忽略 `content/moments` 下的位图，避免 Hugo 把它们当内容页处理；`static/images/` 已写入 `.gitignore`，**构建前必须**运行本脚本（`run_server.bat`、本地手动 `python scripts/sync_images.py`，CI 在 `hugo` 前已加一步）。

全站共用图（如 `homeHeroImage`、`header_img`）请放在 **`content/posts/images/header_img/`** 等子目录，同步后路径仍为 `/images/header_img/...`。

#### 8.2.1 已从 Git 跟踪 `static/images` 的仓库

首次启用忽略后执行：`git rm -r --cached static/images`，再提交；磁盘上无需删文件，下次 `sync_images` 会重新生成。

### 8.3 同步图标：`scripts/sync_icons.py`

站点图标也走一个小同步脚本，把 `my_icons/` 里的图复制到 `static/`：

```python
ico = src_dir / "favicon.ico"
png = src_dir / "favicon.png"

copy(ico, out_dir / "favicon.ico")
copy(png, out_dir / "favicon.png")
for name in ["favicon-16x16.png", "favicon-32x32.png", "apple-touch-icon.png"]:
    copy(png, out_dir / name)
```

这样图标源文件只维护一份，发布目标格式统一在 `static/` 里产出。

### 8.4 本地启动入口：`run_server.bat`

本地开发入口不是裸跑 `hugo server`，而是先同步资源，再启动服务器。

```bat
if exist ".env" (
  for /f "usebackq eol=# tokens=1,* delims==" %%A in (".env") do (
    if not "%%A"=="" set "%%A=%%B"
  )
)

if not "%~1"=="" (
  set "MOMENTS_PASSWORD_PLAIN=%~1"
  for /f "usebackq delims=" %%H in (`powershell -NoProfile -Command "$s=$env:MOMENTS_PASSWORD_PLAIN; $bytes=[System.Text.Encoding]::UTF8.GetBytes($s); $hash=[System.Security.Cryptography.SHA256]::Create().ComputeHash($bytes); -join ($hash | ForEach-Object { $_.ToString('x2') })"`) do set "HUGO_MOMENTS_PASSWORD_HASH=%%H"
)

set "HUGO_BASEURL=http://localhost:1313/"
hugo server -D --buildFuture --baseURL "http://localhost:1313/" --appendPort=false
```

这说明本地运行流程是：

#### 8.4.1 第一步，同步图片

保证 Markdown 里的 `./images/...` 在网页里真能访问到。

#### 8.4.2 第二步，同步 favicon

保证图标资源最新。

#### 8.4.3 第三步，可用命令行参数临时覆盖动态区口令

`run_server.bat 你的密码` 会先在本地把明文算成 `SHA-256` 哈希，再注入 `HUGO_MOMENTS_PASSWORD_HASH`，模板里不会出现明文口令。

#### 8.4.4 第四步，本地覆盖 `baseURL`

避免配置文件里的线上地址影响本地调试。

#### 8.4.5 第五步，带 `--buildFuture`

未来日期文章也参与预览。

## 9 额外的前端运行时

### 9.1 `static/js/pretext-runtime.js`

这是仓库里最像“前端业务 JS”的文件，不属于 Hugo 模板层，而是发布后在浏览器里运行。

文件开头先说明了它的目标：

```js
// Pretext runtime (按需启用)：只有当页面出现标记元素时才会加载 @chenglou/pretext
// 标记：
// - data-pretext-height="1"：A：计算文本高度并设置元素高度（避免排版抖动）
// - data-pretext-bubble="1"：B：在保持行数不变的前提下尽量收紧宽度（v1：二分搜索）
```

也就是说，它服务的是某些实验性文章 demo，而不是整个博客的基础框架。

核心加载函数如下：

```js
async function loadPretext() {
  if (window.__pretextLayoutEngine) return window.__pretextLayoutEngine;
  try {
    const engine = await import("https://cdn.jsdelivr.net/npm/@chenglou/pretext@0.0.4/dist/layout.js");
    window.__pretextLayoutEngine = engine;
    return engine;
  } catch (e) {
    console.warn("[pretext-runtime] pretext import failed, fallback mode:", e);
    window.__pretextLayoutEngine = null;
    return null;
  }
}
```

高度计算函数：

```js
async function applyHeight(pretext, el) {
  const innerWidth = getInnerWidth(el);
  const text = getText(el);
  const font = getCanvasFont(el);
  const lineHeightPx = getLineHeightPx(el);

  if (pretext && typeof pretext.prepare === "function" && typeof pretext.layout === "function") {
    const prepared = pretext.prepare(text, font);
    const out = pretext.layout(prepared, innerWidth, lineHeightPx);
    textHeight = out.height;
    lineCount = out.lineCount;
  }
}
```

宽度收缩函数：

```js
async function applyBubble(pretext, el) {
  const prepared = pretext.prepare(text, font);
  const target = pretext.layout(prepared, innerWidth0, lineHeightPx).lineCount;

  let low = Math.max(80, innerWidth0 * 0.25);
  let high = innerWidth0;

  for (let i = 0; i < 18; i++) {
    const mid = (low + high) / 2;
    const { lineCount } = pretext.layout(prepared, mid, lineHeightPx);
    if (lineCount === target) {
      high = mid;
    } else {
      low = mid;
    }
  }
}
```

这部分底层逻辑可以概括成：

#### 9.1.1 只有页面里出现 `data-pretext-*` 标记时才启用

平时不打扰普通文章。

#### 9.1.2 远程动态导入布局引擎

降低首屏负担。

#### 9.1.3 用浏览器端重新测量文本布局

实现一些普通 CSS 很难做的“宽高联动排版实验”。

## 10 一个很实用的小 shortcode

### 10.1 `layouts/shortcodes/post_link_by_title.html`

这个短代码会按照文章标题查站内页面，再输出链接。

```html
{{- $title := .Get "title" -}}
{{- $pages := where site.RegularPages "Title" $title -}}
{{- $p := index $pages 0 -}}

{{- if $p -}}
  <a href="{{ $p.RelPermalink }}" class="{{ .Get `class` | default `year-end-link` }}">{{ $text }}</a>
{{- else -}}
  <span class="{{ .Get `class` | default `year-end-link` }}">{{ $text }}</span>
{{- end -}}
```

这个设计很适合年终总结、系列文章互链，因为作者只要记文章标题，不必每次手敲 URL。

## 11 底层工作流总结

### 11.1 从内容到页面的完整链路

你可以把这个博客的底层逻辑理解成下面这条流水线：

```text
content/*.md / data/*.yaml
        ↓
hugo.toml 提供全局参数
        ↓
themes/PaperMod/layouts/_default/baseof.html 提供页面骨架
        ↓
layouts/_default/*.html / layouts/projects/list.html 决定主内容区
        ↓
layouts/partials/*.html 注入侧栏、目录、评论、页脚增强
        ↓
assets/css/extended/custom.css 统一改样式
        ↓
static/* 原样发布
        ↓
public/* 生成最终站点
```

### 11.2 这个博客最核心的四层分工

#### 11.2.1 第一层：内容层

`content/` 里的 Markdown 页面和 `data/projects.yaml`。

#### 11.2.2 第二层：模板层

`layouts/` 负责把内容变成 HTML 结构。

#### 11.2.3 第三层：样式层

`assets/css/extended/custom.css` 负责把结构变成当前这个视觉效果。

#### 11.2.4 第四层：运行时增强层

`footer.html`、`extend_footer.html`、`pretext-runtime.js` 负责浏览器中的交互行为。

### 11.3 这个仓库为什么看起来“比普通 Hugo 博客复杂”

因为它不是只改皮肤，而是同时做了这些事：

1. 覆盖了首页、文章页、标签页、统计页、projects 页多个模板。
2. 给博客加了双栏侧栏系统、手写 TOC、阅读进度条、Gitalk 延迟加载。
3. 用 render hook + Python 同步脚本重构了图片工作流。
4. 额外托管了 `static/scripts/` 下的工具型静态页面。

所以从项目性质看，它已经不是“纯主题配置型博客”，而是一个带内容系统、数据页、静态工具页和前端增强脚本的 Hugo 站点。

## 12 动态流与口令保护

### 12.1 内容目录与页面模板

新的“动态”不再来自 `content/now.md`，而是单独放在 `content/moments/` 下，每一条都是一篇独立 Markdown 条目。

`content/moments/_index.md`：

```md
---
title: "动态"
description: "一个带时间刻度的私密动态流，内容源在 content/moments/。"
comments: false
ShowToc: false
hideMeta: true
---
```

列表页与单条页模板分工如下（与仓库实际文件一致）：

| 文件 | 作用 |
|------|------|
| `layouts/moments/list.html` | 动态流首页：顶部 `moments-hero`（公开）、口令区 `moments-gate`、`data-moments-feed` 内按时间倒序渲染卡片 |
| `layouts/moments/single.html` | 单条动态：同一套 gate，正文在 `data-moments-feed` 内 |
| `layouts/partials/moments_card.html` | 单条卡片结构（时间轨、正文、`photos` 图库、标签等） |

构建时从环境变量读取哈希与找回邮箱，并写入根节点 `data-*`（见下节）。列表模板在口令校验通过前，会给 `section.moments-feed` 加 HTML `hidden`；样式层在 `assets/css/extended/custom.css` 里用 `[data-moments-gate-root]:not(.is-unlocked) [data-moments-feed] { display: none !important; }` 再锁一层，避免个别环境下 `[hidden]` 被其它 CSS 间接盖过导致「未输口令却看到动态正文」。

**注意**：顶栏 `moments-hero` 里的标题、描述、`_index.md` 正文属于刻意保留的公开导语，不是「被口令保护的动态条目」；口令只遮挡 `data-moments-feed` 里的条目列表或单条正文。

也就是说，动态流不是一篇 Markdown 长文，而是：

1. `content/moments/*.md` 存条目。
2. `layouts/moments/list.html` / `single.html` 负责渲染与 gate 结构。
3. `layouts/partials/moments_card.html` 负责每一张动态卡片。

### 12.2 动态卡片和图片支持

单条动态卡片 partial 如下：

```html
<article class="moments-card">
  <div class="moments-card__rail">
    <time class="moments-card__date" datetime="{{ $srcPage.Date.Format "2006-01-02T15:04:05Z07:00" }}">{{ $srcPage.Date.Format "01/02" }}</time>
  </div>

  <div class="moments-card__body">
    <div class="moments-card__meta">
      <span class="moments-stamp">{{ $srcPage.Date.Format "2006-01-02 15:04" }}</span>
      <span class="moments-chip moments-chip--accent">{{ $srcPage.Params.mood }}</span>
    </div>
    <div class="moments-card__content post-content">
      {{ $srcPage.Content }}
    </div>
    <div class="moments-gallery">
      <img src="{{ $img }}" alt="{{ $srcPage.Title }}" loading="lazy" />
    </div>
  </div>
</article>
```

这里支持两类内容：

1. 正文直接写 Markdown，时间由 `date` 字段提供。
2. 图片可放在 front matter 的 `photos` 数组里，模板会自动排成卡片图库。

### 12.3 密码不写明文，而是构建时注入哈希

`hash`值获取方法

```bat
# 方法一
powershell -NoProfile -File scripts\moments_password_hash.ps1 "code"

# 方法二
$s = "你的口令"
$bytes = [System.Text.Encoding]::UTF8.GetBytes($s)
$hash = [System.Security.Cryptography.SHA256]::Create().ComputeHash($bytes)
-join ($hash | ForEach-Object { $_.ToString("x2") })

# 方法三
.\run_server.bat code
```



动态页的口令 gate 不是把明文口令写死到模板里，而是构建时读环境变量，把 **SHA-256 小写十六进制哈希** 写进根节点的 `data-password-hash`（见 `layouts/moments/list.html`、`layouts/moments/single.html`）。另可配置 `data-recovery-email`（环境变量 `HUGO_MOMENTS_RECOVERY_EMAIL`，未设时回退到 `site.Params.sidebarEmail`）。

前端逻辑在 `static/js/moments-gate.js`（构建后由 `/js/moments-gate.js` 引用），对用户输入做 `SHA-256` 后与 `data-password-hash` 比较；一致则在根节点加 `class="is-unlocked"`，并去掉 feed 的 `hidden`。

会话策略（与当前实现一致）：

1. 勾选「记住这次设备访问」：解锁标记写入 `localStorage`（键为 `data-session-key`，由哈希派生前缀），关闭浏览器后仍有效。
2. 不勾选：写入 `sessionStorage`，**同一标签页内**从列表点到任意动态子页无需重输；**整页刷新（reload）** 时会先清掉该 `sessionStorage` 键，需重新输入（避免误以为「没记住设备却仍永久免密」）。
3. 若构建时未注入哈希（空字符串），脚本会在状态栏提示设置 `HUGO_MOMENTS_PASSWORD_HASH`，且不会解锁 feed。

本地可把哈希放进忽略提交的 `.env`（`run_server.bat` 会加载），例如：

```env
HUGO_MOMENTS_PASSWORD_HASH=<你的口令的 SHA-256 小写十六进制>
HUGO_MOMENTS_RECOVERY_EMAIL=<站长接收找回邮件的邮箱，可选>
```

本地也可用一行传明文，由批处理现算哈希（不落盘）：

```bat
run_server.bat 你的明文口令
```

**安全边界（静态站）**：HTML 里仍包含被 `hidden` / CSS 遮挡的正文，RSS（如全站 `index.xml`）也可能出现 moments 摘要；口令门主要挡普通浏览，不能替代私有仓库或后端鉴权。

### 12.4 线上部署：GitHub Actions 与 Repository secrets

本站由 `.github/workflows/hugo.yml` 在推送 `main` 时构建并部署 GitHub Pages。构建步骤里已传入：

```yaml
env:
  HUGO_MOMENTS_PASSWORD_HASH: ${{ secrets.HUGO_MOMENTS_PASSWORD_HASH }}
  HUGO_MOMENTS_RECOVERY_EMAIL: ${{ secrets.HUGO_MOMENTS_RECOVERY_EMAIL }}
```

在仓库网页上配置步骤（与 **Settings → Secrets and variables → Actions** 下界面一致）：

1. 打开 GitHub 上本仓库的 **Settings**。
2. 左侧进入 **Secrets and variables** → **Actions**。
3. 选中 **Secrets** 标签页，在 **Repository secrets** 区域点击 **New repository secret**（不要只配 *Environment secrets*，除非你在 workflow 里显式绑定了 environment；当前 workflow 未使用 environment，应使用 **Repository secrets**）。
4. 新建 secret：**Name** 必须为 `HUGO_MOMENTS_PASSWORD_HASH`，**Secret** 填你的口令的 **SHA-256 小写十六进制**（与本地 `.env` 或 `run_server.bat` 生成的值一致）。
5. 可选：再建 `HUGO_MOMENTS_RECOVERY_EMAIL`，供「忘记密码」邮件收件人；不配则线上回退用 `hugo.toml` 里的 `sidebarEmail`。
6. 保存后重新跑一次部署（例如推送任意提交或 Actions 里 **Re-run jobs**），新构建的页面才会带上哈希；仅改 Secret 不触发构建则线上仍是旧 HTML。

生成 SHA-256（PowerShell 示例，UTF-8 与 `run_server.bat` 一致）：

```powershell
$s = "你的明文口令"
$bytes = [System.Text.Encoding]::UTF8.GetBytes($s)
$hash = [System.Security.Cryptography.SHA256]::Create().ComputeHash($bytes)
-join ($hash | ForEach-Object { $_.ToString("x2") })
```

换口令时：更新 Repository secret 中的哈希 → 再触发一次构建部署即可；`git push` 本身不会把 Secret 传到仓库历史里。

### 12.5 忘记密码与邮箱找回的真实边界

当前实现里，“忘记密码”会展开一个邮箱找回表单，再调用默认邮件客户端向站长邮箱发起找回请求：

```js
function buildMailto(targetEmail, requesterEmail) {
  const subject = "动态区密码找回";
  const body = [
    "你好，我想申请找回动态区访问口令。",
    "",
    "联系邮箱：" + requesterEmail,
    "页面：" + location.href
  ].join("\n");
  return "mailto:" + encodeURIComponent(targetEmail) + "?subject=" + encodeURIComponent(subject) + "&body=" + encodeURIComponent(body);
}
```

这能完成“邮箱找回入口”，但做不到真正站内验证码验证。原因很简单：这个项目是纯静态 Hugo 站，没有后端去发验证码、存验证码和校验验证码。

所以这里要区分两件事：

1. 现在已经实现的是“邮件发起找回”。
2. 如果你要真正的“邮箱验证码验证后重置密码”，必须额外接入后端或第三方邮件服务。

## 13 本次修改记录（moments 时间轴与访问体验）

### 13.1 目标

1. 动态列表改为“最新文章在上”。
2. 时间轴改为左侧常驻 sticky，滚动时一直可见，便于随时点击。
3. 时间轴三种粒度（年/月/日）统一显示文章数；“日”按具体日期（`YYYY-MM-DD`）分组，不再按周。

### 13.2 对应源码

#### A. 列表顺序（最新在上）

文件：`layouts/moments/list.html`

```go-html-template
{{- $pages := .RegularPages.ByDate.Reverse -}}
```

#### B. 左侧 sticky 时间轴结构

文件：`layouts/moments/list.html`

```go-html-template
<section class="moments-vlayout" data-moments-feed hidden>
  <aside class="moments-vtimeline" data-moments-vtimeline>
    <div class="moments-vtimeline__head">...</div>
    <div class="moments-vtimeline__body">
      <ul class="moments-vtimeline__list" data-v-list></ul>
    </div>
  </aside>
  <div class="moments-vstage" data-moments-stage>...</div>
</section>
```

#### C. 时间轴分组逻辑（年/月/日）

文件：`static/js/moments-gate.js`

```javascript
if (mode === "year") {
  key = item.year;
  label = item.year + "年";
  tip = item.year + "年";
} else if (mode === "month") {
  if (item.year !== latestYear) return;
  key = item.month;
  label = item.month.slice(5) + "月";
  tip = item.month;
} else {
  if (item.year !== latestYear) return;
  key = item.day;
  label = item.day.slice(5);
  tip = item.day;
}
```

#### D. 时间轴计数显示（年/月/日都显示）

文件：`static/js/moments-gate.js`

```javascript
var count = document.createElement("small");
count.className = "moments-vtimeline__count";
count.textContent = g.count + "篇";
anchor.appendChild(count);
```

#### E. sticky 样式与字体放大

文件：`assets/css/extended/custom.css`

```css
.moments-vlayout {
  display: grid;
  grid-template-columns: 236px minmax(0, 1fr);
  gap: 0.8rem;
}

.moments-vtimeline {
  position: sticky;
  top: calc(var(--header-height, 60px) + 0.8rem);
  margin-left: 6px;
}

.moments-vtimeline__mode { font-size: 0.9rem; }
.moments-vtimeline__label { font-size: 0.95rem; }
.moments-vtimeline__count { font-size: 0.78rem; }
```

### 13.3 交互说明

1. 点击时间轴条目会平滑滚动到对应 `#moment-*` 文章锚点。
2. 页面滚动时会自动高亮当前可视文章对应的时间轴条目。
3. 在窄屏（`max-width: 640px`）下，时间轴回退为普通上方块布局（不强制 sticky）。

### 13.4 本次最终版源码（完整关键段）

以下是当前仓库里已生效的最终版源码片段（不是仅文件位置）：

#### 13.4.1 `layouts/moments/list.html`（动态列表 + 左侧时间轴骨架）

```go-html-template
<section class="moments-vlayout" data-moments-feed hidden>
  {{- $pages := .RegularPages.ByDate.Reverse -}}
  {{- if gt (len $pages) 0 -}}
  <aside class="moments-vtimeline" data-moments-vtimeline>
    <div class="moments-vtimeline__head">
      <p class="moments-vtimeline__eyebrow">Timeline</p>
      <div class="moments-vtimeline__modes" role="tablist" aria-label="时间轴粒度">
        <button class="moments-vtimeline__mode" type="button" data-v-mode="year">年</button>
        <button class="moments-vtimeline__mode is-active" type="button" data-v-mode="month">月</button>
        <button class="moments-vtimeline__mode" type="button" data-v-mode="day">日</button>
      </div>
    </div>
    <div class="moments-vtimeline__body">
      <ul class="moments-vtimeline__list" data-v-list></ul>
    </div>
  </aside>

  <div class="moments-vstage" data-moments-stage>
    {{- range $idx, $p := $pages -}}
    {{- $aid := printf "moment-%d" $idx -}}
    <section
      id="{{ $aid }}"
      class="moments-feed__item"
      data-moment-id="{{ $aid }}"
      data-moment-ts="{{ $p.Date.Unix }}"
      data-moment-year="{{ $p.Date.Format "2006" }}"
      data-moment-month="{{ $p.Date.Format "2006-01" }}"
      data-moment-day="{{ $p.Date.Format "2006-01-02" }}"
      data-moment-weekday="{{ $p.Date.Weekday }}"
      data-moment-title="{{ $p.Title }}"
    >
      {{- partial "moments_card.html" $p -}}
    </section>
    {{- end -}}
  </div>
  {{- end -}}
</section>
```

#### 13.4.2 `static/js/moments-gate.js`（时间轴分组/渲染/点击跳转）

```javascript
function toModeGroups(items, mode) {
  if (!items.length) return [];
  var latestYear = items[items.length - 1].year;
  var rows = [];
  var map = {};

  items.forEach(function (item) {
    var key = "";
    var label = "";
    var tip = "";
    if (mode === "year") {
      key = item.year;
      label = item.year + "年";
      tip = item.year + "年";
    } else if (mode === "month") {
      if (item.year !== latestYear) return;
      key = item.month;
      label = item.month.slice(5) + "月";
      tip = item.month;
    } else {
      if (item.year !== latestYear) return;
      key = item.day;
      label = item.day.slice(5);
      tip = item.day;
    }

    if (!map[key]) {
      map[key] = { key: key, label: label, tip: tip, target: item.id, count: 0, firstTs: item.ts };
      rows.push(map[key]);
    }
    map[key].count += 1;
  });

  return rows;
}

function initVerticalTimeline(root) {
  if (!root || root.dataset.vTimelineReady === "1") return;
  var shell = root.querySelector("[data-moments-vtimeline]");
  var list = root.querySelector("[data-v-list]");
  var modeBtns = Array.prototype.slice.call(root.querySelectorAll("[data-v-mode]"));
  var posts = Array.prototype.slice.call(root.querySelectorAll(".moments-feed__item[data-moment-id]"));
  if (!shell || !list || !posts.length) return;

  var items = posts.map(function (node) {
    var ts = Number(node.dataset.momentTs || 0) * 1000;
    var d = new Date(ts);
    return {
      id: node.dataset.momentId || "",
      title: node.dataset.momentTitle || "",
      ts: ts,
      date: d,
      year: node.dataset.momentYear || "",
      month: node.dataset.momentMonth || "",
      day: node.dataset.momentDay || "",
      node: node
    };
  }).filter(function (x) { return x.id; });
  if (!items.length) return;

  var activeMode = "month";
  var activeId = items[items.length - 1].id;

  function setActiveDotById(targetId) {
    Array.prototype.slice.call(list.querySelectorAll(".moments-vtimeline__item")).forEach(function (li) {
      li.classList.toggle("is-active", li.dataset.target === targetId);
    });
  }

  function render() {
    shell.dataset.mode = activeMode;
    modeBtns.forEach(function (btn) {
      var on = btn.dataset.vMode === activeMode;
      btn.classList.toggle("is-active", on);
      btn.setAttribute("aria-selected", on ? "true" : "false");
    });

    list.innerHTML = "";
    var groups = toModeGroups(items, activeMode);
    groups.forEach(function (g) {
      var li = document.createElement("li");
      li.className = "moments-vtimeline__item";
      li.dataset.target = g.target;

      var anchor = document.createElement("a");
      anchor.className = "moments-vtimeline__link";
      anchor.href = "#" + g.target;
      anchor.dataset.target = g.target;

      var dot = document.createElement("span");
      dot.className = "moments-vtimeline__dot";
      dot.setAttribute("aria-hidden", "true");
      dot.setAttribute("data-tip", g.tip);

      var label = document.createElement("span");
      label.className = "moments-vtimeline__label";
      label.textContent = g.label;

      anchor.appendChild(dot);
      anchor.appendChild(label);

      var count = document.createElement("small");
      count.className = "moments-vtimeline__count";
      count.textContent = g.count + "篇";
      anchor.appendChild(count);

      anchor.addEventListener("click", function (e) {
        e.preventDefault();
        var target = document.getElementById(g.target);
        if (!target) return;
        var top = target.getBoundingClientRect().top + window.pageYOffset - getHeaderOffset();
        window.scrollTo({ top: Math.max(0, top), behavior: "smooth" });
        history.replaceState(null, "", "#" + g.target);
        activeId = g.target;
        setActiveDotById(g.target);
      });

      li.appendChild(anchor);
      list.appendChild(li);
    });
    setActiveDotById(activeId);
  }

  function refreshByScroll() {
    var threshold = getHeaderOffset() + 40;
    var current = items[0].id;
    items.forEach(function (item) {
      var rect = item.node.getBoundingClientRect();
      if (rect.top <= threshold) current = item.id;
    });
    activeId = current;
    setActiveDotById(current);
  }

  modeBtns.forEach(function (btn) {
    btn.addEventListener("click", function () {
      activeMode = btn.dataset.vMode || "month";
      render();
    });
  });

  window.addEventListener("scroll", refreshByScroll, { passive: true });
  window.addEventListener("resize", refreshByScroll, { passive: true });
  render();
  refreshByScroll();
  root.dataset.vTimelineReady = "1";
}
```

#### 13.4.3 `assets/css/extended/custom.css`（左侧固定可见 + 字号放大）

```css
.moments-vlayout {
  display: block;
}

.moments-vtimeline {
  position: fixed;
  left: 8px;
  top: calc(var(--header-height, 60px) + 0.8rem);
  width: 236px;
  max-height: calc(100vh - var(--header-height, 60px) - 1.6rem);
  border: 1px solid color-mix(in srgb, var(--border) 85%, transparent 15%);
  border-radius: 14px;
  background: color-mix(in srgb, var(--entry) 94%, #fff 6%);
  box-shadow: var(--shadow-sm);
  display: flex;
  flex-direction: column;
  min-height: 220px;
  z-index: 60;
}

.moments-vtimeline__mode {
  font-size: 0.9rem;
  padding: 0.26rem 0.66rem;
}

.moments-vtimeline__label {
  font-size: 0.95rem;
}

.moments-vtimeline__count {
  font-size: 0.78rem;
}

@media (max-width: 640px) {
  .moments-vtimeline {
    position: static;
    width: auto;
    max-height: 240px;
    margin-left: 0;
  }
}
```
